# =============================================================================
# Factorio Server Environment Variables
# Copy this file to .env and customize as needed
# =============================================================================

# -----------------------------------------------------------------------------
# Factorio Image Configuration
# -----------------------------------------------------------------------------
# The Docker image to use for the Factorio server
FACTORIO_IMAGE=factoriotools/factorio:stable

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Name of the save file (without .zip extension)
SAVE_NAME=world

# Generate a new save if one doesn't exist (true/false)
GENERATE_NEW_SAVE=true

# Load the latest save on startup (true/false)
LOAD_LATEST_SAVE=true

# -----------------------------------------------------------------------------
# Map Generation Settings
# -----------------------------------------------------------------------------
# Map seed for world generation (leave empty for random)
# This value is written to map-gen-settings.json before world creation
MAP_SEED=

# -----------------------------------------------------------------------------
# Mod Portal Authentication
# Required for automatic mod downloads
# Get your token from: https://factorio.com/profile
# -----------------------------------------------------------------------------
USERNAME=
TOKEN=

# Update mods automatically on server start (true/false)
UPDATE_MODS_ON_START=true

# Comma-separated list of mods to exclude from auto-updates
# UPDATE_IGNORE=mod1,mod2,mod3

# -----------------------------------------------------------------------------
# Map Preview Generation (gen-map profile)
# -----------------------------------------------------------------------------
# Number of random map previews to generate
MAP_PREVIEW_COUNT=100

# Scale for map preview (higher = more zoomed out, e.g., 1, 2, 4)
MAP_PREVIEW_SCALE=4

# Size of the map preview in pixels (e.g., 512, 1024, 2048, 4096)
MAP_PREVIEW_SIZE=512

# -----------------------------------------------------------------------------
# Backup Configuration (Borgmatic)
# -----------------------------------------------------------------------------
# Identifier for this server's backup repository
BACKUP_NAME=factorio

# Backup schedule (cron format) - runs inside borgmatic container
# Default: Every 6 hours
BACKUP_CRON=0 */6 * * *
